title: Kustomisasi Tampilan
description: Sesuaikan tampilan Nuxt PDF Kit.
navigation:
  icon: i-lucide-palette
---

Nuxt PDF Kit dibangun dengan Nuxt UI v3 dan Tailwind CSS v4.

## Variabel CSS (Nuxt UI)

Nuxt PDF Kit mengikuti token desain Nuxt UI v3. Anda dapat melakukan tema pada viewer dengan menimpa variabel CSS Nuxt UI secara global atau per-kontainer. Mode gelap ditangani otomatis oleh color mode Nuxt (`useColorMode()`), jadi Anda tidak perlu menambahkan kelas `dark` secara manual.

```css [assets/css/main.css]
/* Tema global */
:root, .light {
  /* Warna brand */
  --ui-primary: var(--ui-color-primary-500);
  /* Permukaan */
  --ui-bg: var(--color-white);
  --ui-bg-muted: var(--ui-color-neutral-50);
  --ui-bg-elevated: var(--ui-color-neutral-100);
  /* Border */
  --ui-border: var(--ui-color-neutral-200);
  /* Teks */
  --ui-text: var(--ui-color-neutral-700);
  --ui-text-muted: var(--ui-color-neutral-500);
  --ui-text-highlighted: var(--ui-color-neutral-900);
}

.dark {
  --ui-primary: var(--ui-color-primary-500);
  --ui-bg: var(--ui-color-neutral-900);
  --ui-bg-muted: var(--ui-color-neutral-800);
  --ui-bg-elevated: var(--ui-color-neutral-800);
  --ui-border: var(--ui-color-neutral-800);
  --ui-text: var(--color-white);
  --ui-text-muted: var(--ui-color-neutral-400);
  --ui-text-highlighted: var(--color-white);
}

/* Penyesuaian per viewer */
.my-viewer {
  /* Contoh: elevated background sedikit ditint dan warna primary */
  --ui-primary: var(--ui-color-primary-600);
  --ui-bg-elevated: color-mix(in oklab, var(--ui-primary) 6%, var(--ui-bg));
}
```

## Kelas Tailwind

Terapkan kelas Tailwind pada kontainer:

```vue
<template>
  <div class="rounded-xl overflow-hidden shadow-2xl">
    <NuxtPdfKit src="/document.pdf" />
  </div>
</template>
```

## Styling Toolbar

```css
.npk-toolbar {
  height: 56px !important;
  background: linear-gradient(to right, #667eea, #764ba2);
}
```

## Styling Halaman

```css
.npk-page {
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  border-radius: 4px;
}
```

## Styling Scoped

```vue
<template>
  <div class="my-viewer">
    <NuxtPdfKit src="/document.pdf" />
  </div>
</template>

<style scoped>
.my-viewer :deep(.npk-toolbar) {
  /* Gunakan warna brand atau latar belakang kustom */
  background: var(--ui-primary);
}
</style>
```
